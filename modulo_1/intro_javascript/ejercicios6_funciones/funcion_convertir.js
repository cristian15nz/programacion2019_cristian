// var kilometros = parseInt(prompt("Digite los kilometros para llevarlos a metros"));

// var metros = kilometros * 1000;

// alert(`${kilometros}Km serian ${metros}m`);

function convertirKilometrosMetros(cantidadDeKilometros) {
    var metros = cantidadDeKilometros * 1000;
    alert(`${cantidadDeKilometros} km llevados metros serian ${metros}`);
}

var kilometros = 7.5;
convertirKilometrosMetros(kilometros);

var kilometros = 10;
convertirKilometrosMetros(kilometros);

var kilometros = 3;
convertirKilometrosMetros(kilometros);

var kilometros = 1;
convertirKilometrosMetros(kilometros);

var kilometros = 0;
convertirKilometrosMetros(kilometros);